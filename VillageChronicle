<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Village Chronicle</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: #2a2a2a;
            color: #fff;
            padding: 0;
            margin: 0;
            overflow: hidden;
        }
        .chronicle-container {
            display: flex;
            height: 100vh;
        }
        .map-panel {
            flex: 1;
            border-right: 2px solid #ffd700;
            position: relative;
        }
        .story-panel {
            width: 400px;
            background: #1a1a1a;
            overflow-y: auto;
            padding: 20px;
        }
        h2 {
            color: #ffd700;
            margin-top: 0;
            font-size: 1.2em;
            border-bottom: 2px solid #ffd700;
            padding-bottom: 10px;
        }
        iframe {
            width: 100%;
            height: 100%;
            border: none;
        }
        .story-item {
            background: #2a2a2a;
            border-left: 3px solid #ffd700;
            padding: 10px;
            margin-bottom: 10px;
            border-radius: 3px;
            font-size: 0.9em;
        }
        .story-time {
            color: #999;
            font-size: 0.8em;
        }
        .story-type {
            display: inline-block;
            background: #444;
            color: #ffd700;
            padding: 2px 6px;
            border-radius: 2px;
            font-size: 0.75em;
            margin-right: 5px;
        }
        .no-stories {
            color: #999;
            text-align: center;
            padding: 30px;
        }
        .loading {
            color: #999;
            text-align: center;
            padding: 30px;
        }
    </style>
</head>
<body>
    <div class="chronicle-container">
        <div class="map-panel">
            <div style="padding: 20px; text-align: center; color: #ffd700;">
                <h1>🏘️ The Living Village Chronicle</h1>
                <p>Map view integration coming soon. Use "Show Village Map" for the full map experience.</p>
            </div>
        </div>
        <div class="story-panel">
            <h2>📖 Recent Stories</h2>
            <div id="stories" class="loading">Loading stories...</div>
        </div>
    </div>

    <script>
        function loadStories() {
            if (typeof google !== 'undefined' && google.script) {
                google.script.run
                    .withSuccessHandler(displayStories)
                    .withFailureHandler(() => displayStories({stories: []}))
                    .getStoryData();
            } else {
                displayStories({stories: []});
            }
        }

        function displayStories(data) {
            const container = document.getElementById('stories');

            if (!data.stories || data.stories.length === 0) {
                container.innerHTML = '<div class="no-stories">No stories yet. Generate a story cycle to begin!</div>';
                return;
            }

            container.innerHTML = data.stories.slice(0, 10).map(story => `
                <div class="story-item">
                    <div class="story-time">${formatTime(story.timestamp)}</div>
                    <div>
                        <span class="story-type">${story.type}</span>
                    </div>
                    <div style="margin-top: 5px;">${story.story}</div>
                </div>
            `).join('');
        }

        function formatTime(timestamp) {
            const date = new Date(timestamp);
            const now = new Date();
            const diff = now - date;
            const minutes = Math.floor(diff / 60000);

            if (minutes < 1) return 'Just now';
            if (minutes < 60) return `${minutes}m ago`;
            if (minutes < 1440) return `${Math.floor(minutes/60)}h ago`;
            return date.toLocaleDateString();
        }

        window.onload = loadStories;

        // Refresh stories every 60 seconds
        setInterval(loadStories, 60000);
    </script>
</body>
</html>
